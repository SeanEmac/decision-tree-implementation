def calc_entropy():
    # Measure of the uncertainty of the variable.
    # Sum of - proportion log proportion

    # Basically for each attribute class we are trying to predict,
    # sum up their - proportion * log(proportion)
    entropy = 1
    return entropy


def calc_info_gain():
    # Reduction in entropy from partitioning at this attribute
    # total entropy of the set - sum of entropy for that value
    info_gain = 1
    return info_gain
